<Page x:Class="MineSweeper.Pages.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MineSweeper.Pages"
      xmlns:helpers="clr-namespace:MineSweeper.Models"
      mc:Ignorable="d" 
            d:DesignHeight="600" d:DesignWidth="600"

      Title="GamePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <Grid Grid.Row="0" Background="LightGray" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="0" x:Name="Timer" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Grid.Column="0"/>
            <Button Content="Reset" x:Name="ResetButton" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Grid.Column="1" Click="ResetButton_Click"/>
            <TextBlock Text="{Binding MineCount}" x:Name="MineCounter" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Grid.Column="2"/>
        </Grid>

        <Viewbox Stretch="Uniform" Grid.Row="1">
            <Grid x:Name="MineFieldContainer">
                <helpers:SquareUniformGrid x:Name="MineFieldGrid"/>
            </Grid>
        </Viewbox>
        <ContentControl x:Name="OverlayHost"
                    Background="#80000000"
            Visibility="Collapsed" Grid.Row="1"/>
    </Grid>
</Page>
